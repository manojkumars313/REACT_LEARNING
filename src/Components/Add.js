import React from "react";
import { increamentCounterAction } from "../Redux/Actions/CounterAction";
import { decrementCounterAction } from "../Redux/Actions/CounterAction";

function Add() {
  return (
    <div>
      <h1>Add</h1>
    </div>
  );
}

export default Add;
